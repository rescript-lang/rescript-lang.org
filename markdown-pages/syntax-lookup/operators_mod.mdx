---
id: "%"
keywords: ["mod", "modulo", "operator", "%", "remainder"]
name: "%"
summary: "This is the `modulo` operator."
category: "operators"
---

The `%` operator calculates the _modulo_ (remainder after division) of two integers.

### Example

<CodeTab labels={["ReScript", "JS Output"]}>

```res
let result = %(7, 4)
```

```js
var result = 3;
```

</CodeTab>
