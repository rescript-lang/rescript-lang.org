---
id: "float-addition"
keywords: ["plus", "add", "addition", "sum", "float"]
name: "+."
summary: "This is the `floating point addition` operator."
category: "operators"
status: "deprecated"
---

> Deprecated, you can just use `+` for both integers and floats since v12.

This operator performs _floating point_ addition.

<CodeTab labels={["ReScript", "JS Output"]}>

```res
let result = 1.3 +. 0.5
```

```js
var result = 1.3 + 0.5;
```

</CodeTab>

For adding _integers_ see the [`+`](./operators_integer_addition.mdx) operator.
