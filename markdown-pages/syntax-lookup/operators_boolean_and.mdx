---
id: "boolean-and"
keywords: ["&&", "and", "boolean", "logical and"]
name: "&&"
summary: "This is the `boolean AND` operator."
category: "operators"
---

The `&&` operator returns `true` only if both sides are `true`.

### Example

<CodeTab labels={["ReScript", "JS Output"]}>

```res
let canPublish = true && true
```

```js
var canPublish = true;
```

</CodeTab>

### References

- [Control Flow](../docs/manual/control-flow.mdx)
