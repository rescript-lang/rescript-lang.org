let useToc = (~category) => {
  // let routes = Routes.mdxRoutes->Array.filter(route =>
  //   switch route.path {
  //   | Some(path) => path->String.includes(category)
  //   | None => false
  //   }
  // )
  ()
}
